server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Djo9111/ecommerce-microservices.git
          search-paths: config-repo

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
